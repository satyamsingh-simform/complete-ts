const config = {
    api: {
        url: "https://api.com",
        timeout: 5000
    },
    theme: {
        darkMode: true,
        fontSize: 16
    },
    version: "1.0.0"
};

// Step 1: type of config
type ConfigType = typeof config